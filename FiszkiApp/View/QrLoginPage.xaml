<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.Maui.Controls"
             xmlns:viewmodel="clr-namespace:FiszkiApp.ViewModel"
             x:Class="FiszkiApp.View.QrLoginPage"
             x:Name="QrLoginPagContent"
             Title="Logowanie QR">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Image Source="{DynamicResource BackgroundImage}" Aspect="AspectFill" Opacity="0.2" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" Grid.RowSpan="2" />
        
        <Grid Grid.Row="0" Padding="20" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Margin="0,10" Text="Zeskanuj kod QR z panelu administratora" FontSize="18"  HorizontalOptions="Center" FontAttributes="Bold" VerticalOptions="Start" />

            <zxing:CameraBarcodeReaderView Grid.Row="1" Margin="10" x:Name="cameraView" CameraLocation="Rear" BarcodesDetected="CameraView_BarcodesDetected" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" />

            <Label Grid.Row="2" Margin="0,10" Text="{Binding StatusMessage}" FontSize="18" HorizontalOptions="Center" FontAttributes="Bold" TextColor="{DynamicResource CameraStatusLabelColor}" />

            <!-- <Button Text="Test QR" Command="{Binding TestQrCommand}" /> -->
        </Grid>

        <Grid Grid.Row="1" VerticalOptions="End">
            <Grid.RowDefinitions>
                <RowDefinition Height="3" />
            </Grid.RowDefinitions>

            <BoxView Grid.Row="0" />
        </Grid>
    </Grid>
</ContentPage>